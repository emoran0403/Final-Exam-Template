import * as React from "react";
import { useState, useEffect } from "react";

const App = (props: AppProps) => {
  useEffect(() => {}, []);

  return <></>;
};

interface AppProps {}

export default App;
